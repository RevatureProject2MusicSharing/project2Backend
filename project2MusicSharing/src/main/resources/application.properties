spring.application.name=project2MusicSharing

#Custom port - 8080 is the default
server.port = 8080

# Database Credentials---------------

spring.datasource.url=jdbc:postgresql://p2team1.cbsegmk0oe5b.us-east-1.rds.amazonaws.com:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=password

# Sprint Data Settings---------------

# This allows us to see SQL running in the console whenever a DB operation happens
# Great for debugging!
spring.jpa.show-sql=true

# Setting our DDL to update the DB when a change happens
spring.jpa.hibernate.ddl-auto=update
# We could have used "create" which would drop and recreate the tables on every app run

# Specify the schema we're using
spring.jpa.properties.hibernate.default_schema=p2actual